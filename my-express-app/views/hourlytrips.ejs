<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Hourly Trips</title>
    <style>
        .container {
            display: flex;
            justify-content: space-around;
        }
        .table-container, .graph-container {
            width: 45%;
        }
        table {
            width: 100%;
            border-collapse: collapse;
        }
        th, td {
            border: 1px solid black;
            padding: 8px;
            text-align: center;
        }
    </style>
</head>
<body>
    <h1>Hourly Trips</h1>
    <div class="container">
        <div class="table-container">
            <h2>Table</h2>
            <table>
                <thead>
                    <tr>
                        <th>Hour</th>
                        <th>Number of Trips</th>
                    </tr>
                </thead>
                <tbody>
                    <% trips.forEach(trip => { %>
                        <tr>
                            <td><%= trip._id %>:00</td>
                            <td><%= trip.count %></td>
                        </tr>
                    <% }) %>
                </tbody>
            </table>
        </div>
        <div class="graph-container">
            <h2>Graph</h2>
            <img src="data:image/png;base64,<%= imageBase64 %>" alt="Hourly Trips Graph" style="width: 100%;">
        </div>
    </div>
</body>
</html>
